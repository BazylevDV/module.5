class House:
    def __init__(self, name):
        self.name = name
        self.number_of_floors = number_of_floors

        def go_to(1, new_floor):
            if new_floor > self.number_of_floors or new_floor < 1:
                print("Такого этажа не сущестует")
                else:
                    print(int(input(self.number_of_floors)))
